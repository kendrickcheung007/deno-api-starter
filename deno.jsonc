{
  "tasks": {
    "start": "deno task run app.ts",
    "dev": "deno task run --watch scripts/dev.ts",
    "run": "deno run --allow-env=DENO_DEPLOYMENT_ID --allow-net --allow-write=./logs,./routes.ts --allow-read"
  }
}