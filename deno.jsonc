{
  "tasks": {
    "start": "deno run --allow-env=DENO_DEPLOYMENT_ID --allow-net --allow-write --allow-read app.ts",
    "dev": "deno run --allow-env=DENO_DEPLOYMENT_ID --allow-net --allow-read --allow-write --watch app.ts"
  }
}